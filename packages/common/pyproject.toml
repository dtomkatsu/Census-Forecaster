[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "census-common"
version = "0.1.0"
description = "Shared data models and utilities for Census-Forecaster packages."
readme = "README.md"
license = { text = "MIT" }
authors = [{ name = "Tom Katsumi", email = "dtomkatsu@gmail.com" }]
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/common"]
